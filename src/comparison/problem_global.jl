# ------------------------------------------------------------------------------
# Makes the contents of 'A' to become a global variable called 'B', of type Int.
# ------------------------------------------------------------------------------

function problem(problem_number)

    global np = Int(problem_number)

end